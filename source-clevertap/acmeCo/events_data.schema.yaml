$defs:
  Meta:
    properties:
      op:
        default: u
        description: 'Operation type (c: Create, u: Update, d: Delete)'
        enum:
        - c
        - u
        - d
        title: Op
        type: string
      row_id:
        default: -1
        description: Row ID of the Document, counting up from zero, or -1 if not known
        title: Row Id
        type: integer
    title: Meta
    type: object
description: Matches Fivetran EVENTS_DATA — key /event_id (SHA-256), all STRING columns.
properties:
  _meta:
    $ref: '#/$defs/Meta'
    description: Document metadata
  event_id:
    title: Event Id
    type: string
  clevertapId:
    anyOf:
    - type: string
    - type: 'null'
    default: null
    title: Clevertapid
  timestamp:
    anyOf:
    - type: string
    - type: 'null'
    default: null
    title: Timestamp
  event_name:
    anyOf:
    - type: string
    - type: 'null'
    default: null
    title: Event Name
  properties:
    anyOf:
    - type: string
    - type: 'null'
    default: null
    title: Properties
required:
- event_id
title: EventsData
type: object
x-infer-schema: true
if:
  required:
  - _meta
  properties:
    _meta:
      required:
      - op
      properties:
        op:
          const: d
then:
  reduce:
    strategy: merge
    delete: true
