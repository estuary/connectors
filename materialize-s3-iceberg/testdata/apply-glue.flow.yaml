materializations:
  acmeCo/tests/materialize-s3-iceberg:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config-glue.yaml
    bindings: []
