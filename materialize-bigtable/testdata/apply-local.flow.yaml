materializations:
  acmeCo/tests/materialize-bigtable:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config.local.yaml
    bindings: []
