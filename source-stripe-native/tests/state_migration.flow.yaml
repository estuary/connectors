---
# Minimal flow file for testing exempt stream state migration
import:
  - ../acmeCo/flow.yaml
captures:
  acmeCo/source-stripe-native:
    endpoint:
      local:
        command:
          - python
          - "-m"
          - source_stripe_native
        config: ../config.yaml
    bindings:
      - resource:
          name: Accounts
        target: acmeCo/Accounts
    shards:
      logLevel: debug
