---
$defs:
  Meta:
    properties:
      op:
        default: u
        description: "Operation type (c: Create, u: Update, d: Delete)"
        enum:
          - c
          - u
          - d
        title: Op
        type: string
      row_id:
        default: -1
        description: "Row ID of the Document, counting up from zero, or -1 if not known"
        title: Row Id
        type: integer
    title: Meta
    type: object
properties:
  _meta:
    allOf:
      - $ref: "#/$defs/Meta"
    default:
      op: u
      row_id: -1
    description: Document metadata
  WHENMODIFIED:
    format: date-time
    title: Whenmodified
    type: string
  RECORDNO:
    title: Recordno
    type: integer
required:
  - WHENMODIFIED
  - RECORDNO
title: DynamicRecordModel
type: object
x-infer-schema: true
