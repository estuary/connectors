materializations:
  acmeCo/tests/materialize-databricks:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config.local.yaml
    bindings: []
