---
captures:
  acmeCo/source-salesforce:
    endpoint:
      local:
        command:
          - python
          # - "-Xfrozen_modules=off"
          # - "-m"
          # - debugpy
          # - "--listen"
          # - "0.0.0.0:5678"
          # - "--wait-for-client"
          - "-m"
          - source-salesforce
        config: config.yaml
    bindings: []
    shards:
      disable: true
