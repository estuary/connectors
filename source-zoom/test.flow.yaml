---
captures:
  acmeCo/source-zoom:
    endpoint:
      local:
        command:
          - python
          - "-m"
          - source-zoom
        config:
          access_token: "dadsfads"
	  env:
	    LOG_LEVEL: debug
    binding: