info:
  name: cursor page
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/events/guests/list?event_id={{event_id}}&pagination_limit=1&sort_column=created_at&sort_direction=asc"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  **REFERENCE:** https://docs.luma.com/reference/get_v1-events-guests-list
  
  **WHY:** proves the same `pagination_limit`/`has_more`/`next_cursor` contract
  as events holds on the guests endpoint, and that `sort_column=created_at` is
  accepted (the connector sorts for a deterministic walk).
  
  **VERIFIED (2026-09-17):** `200`, 1 entry, `has_more: true`, `next_cursor`
  populated.
