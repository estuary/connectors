info:
  name: BuyingTypes C top_view alone
  type: http
  seq: 11

http:
  method: GET
  url: '{{base_url}}/campaign/get/?advertiser_id={{advertiser_id}}&filtering={"buying_types":["RESERVATION_TOP_VIEW"],"primary_status":"STATUS_ALL"}&page=1&page_size=1000'

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Establishes whether RESERVATION_TOP_VIEW is accepted as a sole value, and whether a
  `page_size` of 1000 is rejected or silently clamped when it is.
