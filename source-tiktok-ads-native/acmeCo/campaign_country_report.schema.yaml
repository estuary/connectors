$defs:
  Meta:
    properties:
      op:
        default: u
        description: 'Operation type (c: Create, u: Update, d: Delete)'
        enum:
        - c
        - u
        - d
        title: Op
        type: string
      row_id:
        default: -1
        description: Row ID of the Document, counting up from zero, or -1 if not known
        title: Row Id
        type: integer
    title: Meta
    type: object
additionalProperties: true
properties:
  _meta:
    $ref: '#/$defs/Meta'
    description: Document metadata
  campaign_id:
    title: Campaign Id
    type: string
  country_code:
    title: Country Code
    type: string
  stat_time_day:
    format: date-time
    title: Stat Time Day
    type: string
  advertiser_id:
    title: Advertiser Id
    type: string
required:
- campaign_id
- country_code
- stat_time_day
- advertiser_id
title: campaign_country_report
type: object
x-infer-schema: true
if:
  required:
  - _meta
  properties:
    _meta:
      required:
      - op
      properties:
        op:
          const: d
then:
  reduce:
    strategy: merge
    delete: true
