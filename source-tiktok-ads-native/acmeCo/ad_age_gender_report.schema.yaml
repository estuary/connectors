$defs:
  Meta:
    properties:
      op:
        default: u
        description: 'Operation type (c: Create, u: Update, d: Delete)'
        enum:
        - c
        - u
        - d
        title: Op
        type: string
      row_id:
        default: -1
        description: Row ID of the Document, counting up from zero, or -1 if not known
        title: Row Id
        type: integer
    title: Meta
    type: object
additionalProperties: true
properties:
  _meta:
    $ref: '#/$defs/Meta'
    description: Document metadata
  ad_id:
    title: Ad Id
    type: string
  age:
    title: Age
    type: string
  gender:
    title: Gender
    type: string
  stat_time_day:
    format: date-time
    title: Stat Time Day
    type: string
  advertiser_id:
    title: Advertiser Id
    type: string
required:
- ad_id
- age
- gender
- stat_time_day
- advertiser_id
title: ad_age_gender_report
type: object
x-infer-schema: true
if:
  required:
  - _meta
  properties:
    _meta:
      required:
      - op
      properties:
        op:
          const: d
then:
  reduce:
    strategy: merge
    delete: true
