---
import:
  - acmeCo/flow.yaml
captures:
  acmeCo/source-microsoft-onedrive:
    endpoint:
      local:
        command:
          - python
          - "-m"
          - source-microsoft-onedrive
        config: connector_config.yaml
    bindings:
      - resource:
          stream: stream_teste
          syncMode: incremental
        target: acmeCo/stream_teste
