materializations:
  acmeCo/tests/materialize-eventbridge/local:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config.local.yaml
    bindings: []
