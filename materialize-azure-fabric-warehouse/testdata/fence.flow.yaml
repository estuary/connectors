materializations:
  acmeCo/tests/materialize-azure-fabric-warehouse:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config.yaml
    bindings: []