collections:
  key/value:
    schema:
      type: object
      properties:
        key: { type: string }
        requiredVal: { type: string }
        optionalVal1: { type: integer }
        optionalVal2: { type: integer }
      required: [key, requiredVal]
    key: [/key]
