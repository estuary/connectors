---
captures:
  acmeCo/source-airtable:
    endpoint:
      # connector:
      #   image: ghcr.io/estuary/source-airtable:local
      #   config:
      #       credentials:
      #         auth_method: api_key
      #         api_key: pat... 
      local:
        command:
          - python
          - "-m"
          - source-airtable
        config:
          credentials:
            auth_method: api_key
            api_key: pat...
    bindings: []
