materializations:
  acmeCo/tests/materialize-redshift:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config.local.yaml
    bindings: []
