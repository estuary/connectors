$defs:
  Meta:
    properties:
      op:
        default: u
        description: 'Operation type (c: Create, u: Update, d: Delete)'
        enum:
        - c
        - u
        - d
        title: Op
        type: string
      row_id:
        default: -1
        description: Row ID of the Document, counting up from zero, or -1 if not known
        title: Row Id
        type: integer
    title: Meta
    type: object
additionalProperties: true
properties:
  _meta:
    $ref: '#/$defs/Meta'
    description: Document metadata
  date:
    format: date-time
    title: Date
    type: string
  organization_id:
    title: Organization Id
    type: string
  actor:
    additionalProperties: true
    title: Actor
    type: object
  actor_id:
    readOnly: true
    title: Actor Id
    type: string
required:
- date
- organization_id
- actor
- actor_id
title: ClaudeCodeUsageRecord
type: object
x-infer-schema: true
if:
  required:
  - _meta
  properties:
    _meta:
      required:
      - op
      properties:
        op:
          const: d
then:
  reduce:
    strategy: merge
    delete: true
