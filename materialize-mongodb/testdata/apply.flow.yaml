materializations:
  acmeCo/tests/materialize-mongodb:
    endpoint:
      local:
        command: ["go", "run", "./cmd/connector"]
        protobuf: true
        config: config.local.yaml
    bindings: []
