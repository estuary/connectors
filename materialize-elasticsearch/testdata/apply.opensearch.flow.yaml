materializations:
  acmeCo/tests/materialize-elasticsearch:
    endpoint:
      local:
        command: ["go", "run", "."]
        protobuf: true
        config: config.opensearch.local.yaml
    bindings: []
