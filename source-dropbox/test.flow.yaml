---
import:
  - acmeCo/flow.yaml
captures:
  acmeCo/source-dropbox:
    endpoint:
      connector:
        image: "ghcr.io/estuary/source-dropbox:local"
        config: source-dropbox.config.yaml
    bindings:
      - resource:
          stream: /Apps/Estuary/test
        target: acmeCo/Apps/Estuary/test
    shards:
      logLevel: debug
