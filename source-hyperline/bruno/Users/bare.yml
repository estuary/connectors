info:
  name: bare
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/users?take=2"
  params:
    - name: take
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 - populated list
    request:
      url: "{{base_url}}/v1/users?take=2"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json
      body:
        type: json
        data: |-
          {
            "meta": {
              "total": "REDACTED",
              "taken": 2,
              "skipped": 0
            },
            "data": [
              {
                "id": "usr_R2zb4x7zgd9xdn",
                "email": "REDACTED",
                "first_name": null,
                "last_name": null,
                "picture_url": "REDACTED",
                "role_id": "rol_ekql2sDuLW8cMl",
                "is_account_owner": false,
                "manager_id": null
              }
            ]
          }

docs: |-
  **REFERENCE:** [List users](https://docs.hyperline.co/api-reference/endpoints/users/list-users.md)

  **WHY:** bare list for the `users` stream. Teaches the live envelope, the
  per-item field set (primary key, created_at/updated_at presence and precision),
  and `meta.total` (dataset size — snapshot sweep cost).

  **VERIFIED (2026-09-04):** meta.total: 13; id prefix `usr_`; items carry NO timestamps at all (id, email, first_name, last_name, picture_url, role_id, is_account_owner, manager_id) — confirmed live, matching OpenAPI. Pure snapshot.
