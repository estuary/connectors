info:
  name: bare
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/taxes/rates?take=2"
  params:
    - name: take
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 - empty list
    request:
      url: "{{base_url}}/v1/taxes/rates?take=2"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json
      body:
        type: json
        data: |-
          {
            "meta": {
              "total": "REDACTED",
              "taken": 0,
              "skipped": 0
            },
            "data": []
          }

docs: |-
  **REFERENCE:** [List tax rates](https://docs.hyperline.co/api-reference/endpoints/taxes/list-tax-rates.md)

  **WHY:** bare list for the `tax_rates` stream. Teaches the live envelope, the
  per-item field set (primary key, created_at/updated_at presence and precision),
  and `meta.total` (dataset size — snapshot sweep cost).

  **VERIFIED (2026-09-04):** endpoint EMPTY on this account — meta `{total: 0, taken: 0, skipped: 0}`.

  **UNOBSERVABLE (until seeded):** item shape unobserved (OpenAPI: oneOf custom/default by `type`; both carry id, code, invoicing_entity_id, created_at, updated_at RFC3339). Close by running `Seeding / D1 - Create Tax Rate` (user-run only), then re-run this request.
